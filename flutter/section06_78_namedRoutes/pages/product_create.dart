import 'package:flutter/material.dart';
//1 aici nu returnam Scaffold, AppBar

class ProductCreatePage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("create a prod"),);
  }
}